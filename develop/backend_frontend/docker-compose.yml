version: "3.8"

services:
  hello-world:
    image: alpine:3.20
    container_name: hello-world
    command: ["sh", "-c", "echo hello-world && sleep infinity"]

networks:
  default:
    name: "${NETWORK}"
    external: true